{
  "buildCommand": "cd my-portfolio && npm install && npm run build",
  "outputDirectory": "my-portfolio/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
} 